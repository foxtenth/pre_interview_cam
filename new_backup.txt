body {
    font-family: Arial, sans-serif;
    line-height: 1.6;
    background-color: #e0f7fa;
    position: relative;
    min-height: 100vh; /* Allows content to overflow */
    display: flex;
    flex-direction: column; /* Makes sure elements are arranged vertically */
    align-items: center; /* Centers content horizontally */
    justify-content: flex-start; /* Align items from the top of the viewport */
    margin: 0;
}

.container {
    background: white;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    position: relative;
    z-index: 2;
    max-width: 900px;
    width: 100%;
    margin-top: 20px; /* Optional: adds top margin to create space from top of the page */
}

@media (max-width: 768px) {
    body {
        display: block; /* Reset to block display for small screens */
        padding: 0 10px; /* Optional: add padding to body for better spacing */
    }

    .container {
        padding: 10px;
        box-shadow: none; /* Remove box-shadow on small screens */
        margin-top: 10px; /* Space between sections */
    }

    h1 {
        font-size: 2em;
    }

    h2 {
        font-size: 1.2em;
    }

    canvas {
        display: none; /* Hide canvas on smaller screens */
    }
}
